# 待确认问题清单

> ⚠️ **本文档状态：已结案 —— 全部条目已由 2026-09-07 定稿（FURNACE_SPEC.md）结案**
>
> - **本清单全部条目已由 2026-09-07 定稿（[FURNACE_SPEC.md](FURNACE_SPEC.md)）结案**；每条下方「✅ 定稿决定」为结案口径，依据 = 定稿条目编号 + [GAP_ANALYSIS.md](GAP_ANALYSIS.md)（D1–D13 决策、§2 差距矩阵、§4 常量表、R0–R6 路线）。
> - 下方原始问题清单与「当前默认决定」表按历史档案保留、不再修订；与定稿冲突之处一律以定稿为准。

> 这些不阻塞架构设计，但进入具体 UI/算法/数据格式实现前需要确认。

> ⏳ **例外：追加「Q3 / Q4」两节（2026-09-25）**，是用户新提出的两项能力（AI 接入、
> MindNet 认知模型接入）。它们**尚未结案、尚未开始实现**，需要先定方案再动代码。

---

## Q3.（待讨论）接入 AI：复刻 agent loop / skill / 工具，但不做 cordis 扩展结构

**用户原话要点**：API 接入 AI，复刻 deepseek harness 的 agent loop / skill / 工具，
但**不需要 cordis 结构**，"弄一个专门的目的明确的软件就行了"。

**已核实的事实**（本会话实测，非推测）：

- 仓库现有代码**完全没有**网络/LLM 依赖：`pubspec.yaml` 无 http/dio/openai 类依赖。
- 应用当前定位是**本地优先、不联网、无账号**（README 与 PRIVACY.md 的核心承诺）。
  AI 接入**直接冲突**于这条承诺，需要用户明确取舍：可选开关？还是改定位？

**待用户决定的问题**：

1. **AI 在这个 app 里做什么？** 候选（可多选）：Thread 事件自然语言解析成任务？
   Knowledge 自动出题/挖空？标签树自动归类？日程自动排？
   —— 不同的用途要求的工具集完全不同，这是动手前必须先定的。
2. **哪家 API / 什么协议？** DeepSeek 官方？OpenAI 兼容端点？本地 Ollama？
3. **API Key 怎么存？** 本地加密存 SQLite？还是每次手填/走环境变量？
   （涉及 PRIVACY.md 的改写。）
4. **"工具"要开放哪些能力？** 只读（查询任务/日程/卡片）还是可写（建任务、改日程）？
   可写就要有确认步骤，否则模型会直接改用户数据。

**"不采用 cordis"的具体含义**（我按理解列出，需用户确认）：不做插件系统 / 不做运行时
扩展注册表 / 不做多 agent 编排 —— 就是一份固定的、写死的工具清单 + 一个单 agent 循环。
即：`对话 → 模型返回工具调用 → 本地执行 → 结果回灌 → 直到完成`，工具在代码里静态声明。

---

## Q4.（待讨论）接入 MindNet 认知模型（只读，不可写）

**硬约束**：`E:\Document\MindNet` **只能读，不能做任何写操作**（用户明确要求）。

**已核实的事实**（本会话只读检查，未做任何修改）：

| 项 | 实测结果 |
| --- | --- |
| 形态 | Node.js 包（`package.json` version `2.0.0-alpha.1`），**没有 pubspec.yaml**，不是 Dart 包 |
| 依赖 | `dependencies` 与 `devDependencies` **均为空** —— 零 npm 依赖 |
| 源码规模 | `src/` 共 **3712 行**（`v2/engine.js` 746、`core/kernel.js` 566、`io/run.js` 574、`diffusion.js` 466、`model.js` 336、`calibration.js` 321、`feedback.js` 316、其余更小） |
| 双端可跑 | `v2/engine.js` 用 `typeof module !== 'undefined'` 判断环境，Node 走 `module.exports`，非 Node 挂到 `globalThis.MindNet` —— **同一份代码能在浏览器里跑**（`viz/index.html` 直接 `file://` 打开即用） |
| 对外接口 | ① 浏览器壳 `viz/index.html`（可导出完整状态 JSON）② CLI `node cli.js <输入.json> --json` ③ 库 `require('./src/index.js')` 导出 `Config` / `CognitiveModel` / `Graph` 等 |
| 输入输出 | 输入 `{ graph:{nodes,edges}, initial_nodes, target_nodes }`，输出知识贡献 KC（Gap/Penalty）与目标激活状态；都是 JSON |

**核心难题**：Furnace 的目标平台包含 **Android**，而 Android 上**没有 Node 运行时**，
本机也没有嵌入 JS 引擎。所以"直接调用 MindNet"最多只能在 Windows 上做 sidecar，
Android 无法复用。

**候选方案（需要用户选或讨论）**：

| 方案 | 说明 | 代价 / 风险 |
| --- | --- | --- |
| A. 移植到 Dart | 把 `src/` 的扩散/记忆动力学按需移植为纯 Dart（仓库已有先例：`ThreadRanker`/`FsrsScheduler`/`DiffusionBoost` 都是纯 Dart 算法层） | 工作量大；必须用 MindNet 的 `test/` + `probe/` 输出做对拍验证，否则会变成"看起来像"的假实现 |
| B. 只取子集 | 只移植本 app 真正要用的那部分（例如扩散 + KC 计算），其余不搬 | 需要先明确用途；子集边界一旦选错返工 |
| C. 不移植，Windows 端 sidecar | Windows 上起 Node 子进程跑 `cli.js --json` | Android 不可用；违背"同一套 Flutter 代码"的现有前提 |
| D. 暂不接入 | 先明确用途再决定 | 不动代码 |

**待用户决定的问题**：

1. **接入 MindNet 要达到什么效果？** 例如：用 KC（发展区/死角）去调整 Knowledge 的
   复习顺序？解释 Thread 排序里"目标匹配/扩散"这一项？还是仅做可视化展示？
   —— 注意仓库**已有**一个简化版扩散（`DiffusionBoost`，图谱距离 BFS + 衰减），
   需要明确它是被 MindNet 取代，还是两者并存。
2. **MindNet 的"图"从哪来？** 是否就是 app 里的标签树（Mindnet 在定稿里已并入标签体系）？
   如果是，节点/边如何映射，边的权重从哪来。
3.（若选 A/B）接受的**对拍口径**是什么：要求 Dart 实现与 JS 实现在给定样例上
   输出完全一致，还是允许数值近似（给一个容差）。

---

## 1. 思维导图交互深度

- [ ] 只需要“树形缩进/折叠”就够，还是需要自由画布、拖拽定位、连线？
  - ✅ 定稿决定：Mindnet 是“成熟的、可编辑的思维导图工具”，支持节点增删改、**拖拽移动**、建立父子/兄弟连接关系（定稿 1.4.1；GAP §2.4 表）；现状树形编辑能力保留，拖拽跨父级 re-parent 列入 R6 打磨（GAP §2.4）。
- [ ] 是否要支持图片、公式、富文本？
  - ✅ 定稿决定：定稿未把导图节点内的图片/公式列为需求（1.4 功能集仅 1.4.1–1.4.3 编辑 / 导入导出 / 标签接口）；现有节点备注等基础能力保留（GAP §2.4 🟢 行）。富文本集中在 Knowledge 词条正文，按 Markdown 为主实现（GAP D5）。

## 2. 任务自动编排算法

- [ ] “建议顺序”的优先级你更看重哪个？
  - 截止时间
  - 依赖关系
  - 预估时长
  - 优先级
  - 时间块精力属性
  - 组合排序
  - ✅ 定稿决定：改为**加权综合评分**——总分 =（紧急度 × 0.4）+（目标匹配度 × 0.3）+（状态适配度 × 0.2）−（同类疲劳惩罚 × 0.1）（定稿 1.1.3；分量归一与缺省常量见 GAP §4.1）；重排**仅**由用户点击“更新与排序”触发（定稿 1.1.3 / GAP D8），旧“任务建议清单”降级为创建时/显式点按的辅助建议（D8）。
- [ ] 是否允许任务自动占用时间块，还是只给建议、用户手动确认？
  - ✅ 定稿决定：**不自动占用、不自动重排**——排序结果只在点击“更新与排序”后呈现（D8）；事件期望/截止时刻与日程块冲突时仅弹窗警告，并提供“移至该日程块之前/之后”一键调整（定稿 1.2.3；GAP §2.2 表）。

## 3. 挖空方式

- [ ] 知识点模板通常由作者手动配置，还是希望软件根据正文“智能挖空”？
  - ✅ 定稿决定：两者并入**自由挖空**机制（定稿 1.3.2）——每次抽卡按概率决定是否创造“历史未出现”的新空位（newClozeProbability = 0.5，GAP §4.2），无新空位则打内部标记、仅从历史方式抽取；作者预设卡（CardTemplates）保留为 legacy，旧 `.kpak` 模板导入后照常生成 preset 卡（GAP §3 / §7 兼容）。
- [ ] 如果智能挖空，你希望优先挖“关键词/术语/数字/人名”中的哪些？
  - ✅ 定稿决定：定稿**未限定**挖空目标词类（无关键词/术语/数字/人名的优先规则）；首批槽位由现有 ClozeGenerator 从既有正文自动推导（GAP §7 兼容节：沿用 ClozeGenerator 逻辑增强），此后“新空位”仅要求历史未出现（定稿 1.3.2）。

## 4. 知识包附件

- [ ] 是否需要支持图片/音频/PDF 等附件？这会影响包体积和导入复杂度。
  - ✅ 定稿决定：**需要，且纳入全量打包**——附件、主题与 SQLite 数据一起压缩为单个 `.tfpkg` 工作区文件（定稿 §3；GAP D4 / §2.6），zip 内含 `attachments/*`（GAP §5 格式要点）；`.kpak` 保留为班级分享子集（不含个人数据，D4）。

## 5. 复习统计

- [ ] 需要哪些统计：连续打卡、每日复习数、正确率、遗忘曲线、预计下次复习？
  - ✅ 定稿决定：调度与“预计下次复习”改由 **FSRS** 完全本地计算（定稿 1.3.2 间隔重复；GAP D9），ReviewLogs / 卡状态扩展 stability、difficulty、due 等 FSRS 字段（GAP §3 数据层表）；既有“复习统计”作为保留能力适配 FSRS（GAP §7），定稿未另立新的报表型统计规格。

## 6. 提醒方式

- [ ] 任务提醒用系统通知就可以，还是需要日历视图/桌面小组件？
  - ✅ 定稿决定：任务提醒保留**系统本地通知**（现状 NotificationService，GAP §2.1 保留项 / §7 保留）；日历视图按定稿 1.2.2 在 Time 模块提供标准日/周/月视图（GAP §2.2 表，R5）；桌面小组件不在定稿范围。

## 7. 名称与视觉

- [ ] 产品最终名称？（暂定“知序 Furnace”）
  - ✅ 定稿决定：**Furnace**（2026-09-07 定稿；GAP D1）——应用标题/文案/ARB 全部替换“知序/Furnace”，Dart 包名最终为 **`furnace`**（本行原写 `threadflow`，为中间名）；工作目录沿用 `class-productivity/`。
- [ ] 视觉风格偏好：简洁冷淡 / 活力彩色 / 护眼深色？
  - ✅ 定稿决定：不做固定单选，改为**高自定义外观系统**：主/辅主题色、背景色/透明度/背景图、UI 与正文字体分设、页面缩放 80%–150%、动画开关；内置官方深/浅默认主题，全部配置打包为可导入导出的 JSON 主题文件（定稿 §4；GAP D13）。

---

## 当前默认决定（如无反馈按此执行）

> ⚠️ 下表为 v0.1 时代的“无反馈默认”，**已全部被 2026-09-07 定稿（FURNACE_SPEC.md）覆盖**，仅作历史存档；执行一律以右列「定稿覆盖」为准。

| 项 | 默认（v0.1） | 定稿覆盖（FURNACE_SPEC / GAP_ANALYSIS） |
| --- | --- | --- |
| 思维导图 | MVP 用树形布局 + 折叠，不做自由画布 | 可编辑导图：节点增删改 / 拖拽移动 / 父子兄弟连接（1.4.1）；.mm / OPML 导入导出（1.4.2）；re-parent 打磨于 R6（GAP §2.4） |
| 自动编排 | 依赖 → 优先级 → 截止时间 → 可用时间块匹配，只给建议 | 加权公式（0.4 / 0.3 / 0.2 / −0.1，定稿 1.1.3；常量 GAP §4.1）；仅“更新与排序”触发（D8） |
| 挖空 | 优先使用作者配置的模板；无模板时基于关键词简单自动挖空 | 自由挖空（概率创造新空位，1.3.2；概率常量 GAP §4.2）；错题强制绑定（D11）；作者预设卡降为 legacy（GAP §3） |
| 附件 | MVP 不支持图片/音频 | 附件随 `.tfpkg` 全量打包（定稿 §3 / GAP D4、§5）；`.kpak` 班级分享子集保留（D4） |
| 统计 | 简单统计：今日待复习、连续天数、正确率 | FSRS 本地计算调度（1.3.2 / D9）；FSRS 字段入表（GAP §3）；既有统计保留并适配（GAP §7） |
| 提醒 | 系统本地通知 | 保留系统本地通知（GAP §2.1 保留项 / §7）；Time 日历视图按 1.2.2 于 R5 提供（GAP §2.2） |
| 名称 | 知序 Furnace（暂定） | **Furnace**（D1，2026-09-07 定稿） |
